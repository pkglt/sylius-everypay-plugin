## Summary

<!-- What changed and why. -->

## Checklist

- [ ] `vendor/bin/phpunit`, `vendor/bin/behat --strict`, `vendor/bin/phpstan analyse` and `vendor/bin/ecs check` pass
- [ ] Translations updated in all four locales (en, lt, et, lv) when keys changed
- [ ] The invariants in AGENTS.md are respected
- [ ] CHANGELOG.md updated under [Unreleased] for user-facing changes
