parameters:
    level: 9

    paths:
        - src
        - tests

    excludePaths:
        # Test application kernel config - contains dependency-version-dependent
        # runtime switches phpstan would judge against the installed set only.
        - tests/TestApplication
